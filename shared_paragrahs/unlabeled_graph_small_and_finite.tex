\begin{definition}[Unlabeled graph \cite{barr1990category}]
    \label{def:graph:unlabeled_graph_small_and_finite}
    % An unlabeled graph that has a set of nodes and arrows is a \textbf{small unlabeled graph}. An unlabeled graph is called \textbf{finite} if it has a finite number of nodes and arrows.
\end{definition}